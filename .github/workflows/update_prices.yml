name: Update MWI Market Data

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest
    # 明確給予內容寫入權限
    permissions:
      contents: write
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # 建議升級到 v4

      - name: Fetch Market Data
        run: |
          curl -L -H "Cache-Control: no-cache" -H "User-Agent: Mozilla/5.0" \
          "https://www.milkywayidle.com/game_data/marketplace.json?t=$(date +%s)" \
          -o data.json

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data.json
          # 檢查是否有變動再 commit
          if git diff --staged --quiet; then
            echo "資料無變動，跳過更新。"
          else
            git commit -m "🚀 自動更新市場單價: $(date)"
            git push
          fi
